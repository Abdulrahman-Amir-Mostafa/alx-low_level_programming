#include <stdio.h>

/**
 * main - main Bodamiru code
 * Description: msh faddy ashr7lk
 * Return: Always 0 (Sucess)
 */

int main(void)
{
	int i;

	for (i = 0; i < 9; i++)
	{
		printf("%i, ", i);
	}
	for (i = 9)
	{
		printf("%i", i);

	printf("\n");
	return (0);
}
